export default function Header({ menuList, currentSection, onChange }) {
  return (
    <header
      style={{
        background: "#222",
        color: "#fff",
        padding: "1rem",
        display: "flex",
        justifyContent: "space-around",
        fontWeight: "bold",
      }}
    >
      sdfsdf
    </header>
  );
}
